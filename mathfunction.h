#include <SDL/SDL.h>
#include "arrays.h"

#ifndef FONCTION_MATH_H
#define FONCTION_MATH_H

double random(double a, double b);
double sigmoid_f(double a);
double dsig_g (double b);
int maths_betwwen (int in, Uint16 l, Uint16 r);

#endif
